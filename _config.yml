# Hexo Configuration
## Docs: https://hexo.io/docs/configuration.html
## Source: https://github.com/hexojs/hexo/

# Site
title: Shadow of the Storm
subtitle: Median-dxz's Blog
description: Median-dxz の blog
author: Median-dxz
language: zh-CN
timezone: Asia/Shanghai

# URL
url: https://median-dxz.xyz
root: /
permalink: :category/:year:month/:title/
permalink_defaults:

pretty_urls:
    trailing_index: true # Set to false to remove trailing 'index.html' from permalinks
    trailing_html: true # Set to false to remove trailing '.html' from permalinks

# Directory
source_dir: source
public_dir: public
tag_dir: tags
archive_dir: archives
category_dir: categories
code_dir: downloads/code
i18n_dir: :lang
skip_render:
    - images/**
    - project/**
    - assets/**

# Writing
new_post_name: :title.md # File name of new posts
default_layout: post
titlecase: false # Transform title into titlecase
external_link:
    enable: true # Open external links in new tab
    field: site # Apply to the whole site
    exclude: ""
filename_case: 0
render_drafts: false
post_asset_folder: false
marked:
    prependRoot: false
    postAsset: true
relative_link: false
future: true
highlight:
    enable: true
    line_number: true
    auto_detect: false
    tab_replace: ""
    wrap: true
    hljs: false
prismjs:
    enable: false
    preprocess: true
    line_number: true
    tab_replace: ""

# Home page setting

# path: Root path for your blogs index page. (default = '')
# per_page: Posts displayed per page. (0 = disable pagination)
# order_by: Posts order. (Order by date descending by default)
index_generator:
    path: ""
    per_page: 10
    order_by: -date

# Category & Tag
default_category: AWastedLand
category_map:
tag_map:

# Metadata elements
## https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meta
meta_generator: true

# Date / Time format
## Hexo uses Moment.js to parse and display date
## You can customize the date format as defined in
## http://momentjs.com/docs/#/displaying/format/
date_format: YYYY-MM-DD
time_format: HH:mm:ss
## updated_option supports 'mtime', 'date', 'empty'
updated_option: "mtime"

# Pagination
## Set per_page to 0 to disable pagination
per_page: 10
pagination_dir: page

# Include / Exclude file(s)
## include:/exclude: options only apply to the 'source/' folder
include:
exclude:
ignore:

# Extensions
## Plugins: https://hexo.io/plugins/
## Themes: https://hexo.io/themes/
theme: next

search:
    path: search.xml
    field: post
    content: true
    format: html

# Deployment
## Docs: https://hexo.io/docs/one-command-deployment
deploy:
    - type: git
      repo: git@git.coding.net:median-dxz/Median_dxz_Hexo_Blog.git

# aplayer:
# cdn: http://xxx/aplayer.min.js                # 引用 APlayer.js 外部 CDN 地址 (默认不开启)
# style_cdn: http://xxx/aplayer.min.css         # 引用 APlayer.css 外部 CDN 地址 (默认不开启)
# meting: true                                  # MetingJS 支持
# meting_api: http://xxx/api.php                # 自定义 Meting API 地址
# meting_cdn: http://xxx/Meing.min.js           # 引用 Meting.js 外部 CDN 地址 (默认不开启)
# asset_inject: true                            # 自动插入 Aplayer.js 与 Meting.js 资源脚本, 默认开启

# img_optimization:
#   exclude:
#     - css/ # anything that ends with a forward slash will be considered a directory
#     - fancybox/
#     # - banner.jpg # anything else is a file
#   jpg:
#     enabled: true # if false, jpgs will get ignored. Defaults to true
#     params: -resize 760000@ # parameter string to pass to imagemagick's convert function
#   png: # jpg/png/jpeg all have the same fields and can be configured independently
#     enabled: false
#     thumb: -resize 1920x1080> -strip -quality 82
#   jpeg:
#     enabled: false
#   # these parameters will be used for any file types that do not have a params parameter defined in config. You can override these to your own preferred defaults.
#   default_params: -resize 760000@ -filter Triangle -define filter:support=2 -unsharp 0.25x0.25+8+0.065 -dither None -posterize 136 -quality 85 -define jpeg:fancy-upsampling=off -interlace none -colorspace sRGB -strip -define png:compression-filter=5 -define png:compression-level=9 -define png:compression-strategy=1 -define png:exclude-chunk=all
#   default_thumb: -resize 1920x1080> -sampling-factor 4:2:0 -strip -quality 85 -interlace JPEG -colorspace RGB
#  https://github.com/vkuznecovas/hexo-img-optimization

symbols_count_time:
    symbols: true
    time: false
    total_symbols: true
    total_time: true

three:
  enable: true
  defer: true
  cdn:
  waves:
    enable: false
    cdn:
  lines:
    enable: true
    cdn: https://cdn.jsdelivr.net/gh/median-dxz/hexo-next-three@4641638479ebfaf0a8bef101e5f975454e0fce30/src/lines.min.js
  sphere:
    enable: false
    cdn:
    
pandoc:
  extensions:
    - '-implicit_figures'